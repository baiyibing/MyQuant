请继承上传文件中的TopkDropoutStrategy类，在买单中过滤掉过去5交易日内涨幅超过15%的股票

这个类继承自TopkDropoutStrategy，并添加了以下关键功能：

在买入逻辑中添加了过滤机制，只买入过去5个交易日内涨幅不超过15%的股票
使用qlib.data.D获取历史收盘价数据
计算过去5个交易日的涨幅（使用shift(4)，因为shift(0)是当前日期）
处理了可能的NaN值（用0填充）
保持了原有的策略逻辑，只在买入股票前添加了过滤条件
这个实现确保了在买入时会过滤掉过去5个交易日内涨幅超过15%的股票，同时保持了原有策略的其他功能不变。



作为职业金融工程师与qlib专家，仔细认真分析上传的程序文件，对上传代码的全面审查，
按针对中国A股的回测系统的准则以及业内 qlib 最佳实践，
提出一个综合优化方案，并评估方案的可行性、必要性、好处、缺点

作为职业金融工程师、一个通达信专家与qlib专家， 请评估现在Alpha158CostKDJ中对以下通达信公式：L1:=COST(0.01);
L2:=COST(99.99);
L3:=(C-L1)/(L2-L1)*100;
K:SMA(L3,3,1),COLORWHITE;
D:SMA(K,3,1),COLORYELLOW;
J:3*K-2*D,COLORFF00FF;
MAIRU:=CROSS(J,K) AND J<80;的实现吻合程度，有什么优化建议