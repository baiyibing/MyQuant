# 任务书：M5 二轮——扩展窗预测导出（2026-03~09 三源长窗归因，MyQuant 侧）

- 日期：2026-09-13
- 背景：M5 首轮（路线图 §0.2）三源同窗只有 16 天（20260303~0323）、名单几乎不重叠、单窗 NAV 不能定胜负。
  数据现已到 2026-09-14（`docs/qlib-data-state-2026-09-15.md`），backtrader 仓湖数据同覆盖——**二轮 = 六个月长窗**。
- 分工：本仓（Codex）做任务 1/2 交付名单；backtrader 仓三源对照（任务 3）由另一会话按交接规格执行。
- 约束沿用 `docs/plan-midterm-m1m4m2m3-2026-09-13.md` §0（基线 **120 passed + 18 subtests**；host_env 已集中 env；全量测试门槛）

---

## 任务 1：`my_scripts/predict_extended.py`（扩展窗预测，不跑 PortAna）

复用 `sweep_live_adapter.py` 的构建模式（一次 handler_init 覆盖全程）：

- CLI：`--train 2026-01-01:2026-01-31 --valid 2026-02-01:2026-02-28 --test 2026-03-01:2026-09-14`
  （缺省即上述值；`START:END` 校验与非法拒绝，参考 sweep_ranking 现有实现）
- handler 覆盖三段 min/max（label 末端 NaN dropna，与现行为一致，注释说明）
- 模型与 processors 同 M3-A 配置（含 `DropLimitUpLearn` 的 module_path 接线），**不跑 PortAna/不对齐自检**
  （那是训练脚本的烟雾段；本任务只出 pred，省时间）
- 产物：`预测结果_ext.csv`（CWD，同现有格式 datetime,instrument,score）+ train manifest（segments 必含窗口）
- 验收：单测（分段校验/参数解析）；宿主跑通一次（~25 分钟，8 个月 handler），pred 日期覆盖约 128+ 个交易日

## 任务 2：导出三源对照的 Qlib 臂

```bash
python my_scripts/export_daily_pool.py --pred my_scripts/预测结果_ext.csv \
    --out-dir exports/m5r2_pred_topn10_20260302_20260914
```

- as-of=pred_minus_one（锁死），topk=10（**M5 归因名单源，不加 topk 臂**——那是 M3-B 的维度，混了会污染归因）
- export manifest 自动落（M4-C 已接）
- 验收：导出天数 = pred 天数 −1（首日无前日 pred）；字节契约测试已有，绿即可

## 任务 3（backtrader 仓，另一会话执行——交接规格）

三源同窗 `20260303~20260914`，6/8 策略书对照（复用 m5 工具 `--out-dir` / `m5_hand_topn`）：
- 源 A：`MyQuant/exports/m5r2_pred_topn10_20260302_20260914/`（本任务书产物，注意跨仓拷贝或直读路径）
- 源 B：`stock_pool/`（手工）
- 源 C：hand10
- 出归因报告（命中率/换手/重叠度三列表），回写路线图 §0.2

## 判读纪律（预锁）

- 长窗结论仍非产品结论：只回答「Qlib 名单源在长窗下是否稳定优于/劣于手工与随机基线」
- **不据此改 topk/换手参数**——改参属 M3-B 维度，需第三窗佐证（OOS 已证明单窗会骗人）
- 三源名单重叠度随窗长变化本身是结论的一部分（首轮几乎不重叠，长窗是否依然）

## 明确不做

- 不重开 as-of、不动池字节契约、不跑 PortAna、不造第四套引擎
