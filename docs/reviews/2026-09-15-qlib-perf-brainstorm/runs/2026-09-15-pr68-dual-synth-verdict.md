# PR #68 · 双源并版裁决 · PR67 round2 之后下一刀

**日期**：2026-09-15 晚  
**线上默认**：**仍 10/3**。本页任何数字都不构成改 topk / 默认开过滤 / 把 `10/3+ST` 写成已上线的依据。  
**主 pred**：recorder `8a061ea428e04bb3a199a485ade49d0e`

## 来源（真 CLI，非代写）

| 席位 | CLI | 模型 | 产物 |
|---|---|---|---|
| Codex | 原生 `codex exec` | `gpt-5.6-sol` + `model_reasoning_effort=high` | `runs/2026-09-15-pr68-codex-sol-high-verdict.md` |
| Cursor Opus 5 | `cursor-agent -p` | `claude-opus-5-thinking-high` | `runs/2026-09-15-pr68-cursor-opus-verdict.md` |
| 上游 | — | — | `runs/2026-09-15-pr67-round2-handoff.md` |

本页是 **Grok 并版**：一致处钉死；Q1 分歧写明取舍。

---

## 0. 一句话（并版）

**本周「动手」的唯一主刀是 Opus 的 R2-D：零回测把打死 50/5 的 S1/S2 段检尺子架到线上宽度（S1′/S2′），并用换手线性成本闭式补 S3；同时并行推进 Codex 的 2024 第三窗预锁设计（本周只设计、不训不跑）。`88.5×` 换手不单独开刀；训练侧去 ST 仍与第三窗合并；线上仍 10/3；`50/5+ST` 不抬回 shortlist。**

**2026-09-15 夜回写**：R2-D 已跑。G1/G2 过，G3 死在单月条款，G4 闭式差 0.83pp。打 `ST_GATE_MONTH_CONCENTRATED` + `ST_GATE_SUBPERIOD_ONLY_ONLINE_WIDTH` + `COST_DERIVATION_INVALID`。宿主意见「能训就训」→ 预锁落地并开 2024 两臂；主臂改训练侧臂 B。页：`2026-09-15-pr68-r2d.md`、`2026-09-15-pr68-r3-prelock.md`。

---

## 1. 两边一致（直接采纳）

1. **成本不单独开刀**：同窗三档已齐；再跑一遍 `COST_TIERS` 信息量近零。成本并进第三窗预锁的现实档门；执行层可成交性（ADV/冲击）按 N5 另立项、本周只登记。
2. **训练侧去 ST 仍与 2024 第三窗合并、本周只设计**：S4 过 ≠ 立刻改训练宇宙；单独重训会毁掉 `8a061ea4` 同 pred 对照（N2）。
3. **纪律**：线上仍 10/3；不覆盖 `8a061ea4` pred/原包；不开买入状态；`50/5+ST` 维持 `ST_GATE_SUBPERIOD_ONLY`，不抬回 shortlist；禁 n_drop 细表（失资格仍持仓 20.7%）。
4. **`10/3+ST +20.7%` 身份**：研究档 / 线上宽度待独立窗验证的增量；不是可上线方案。两段约 +22% 同号，但仍属同一 2026 全窗。

---

## 2. Q1 分歧与并版取舍

| | Codex sol+high | Cursor Opus 5 | **并版** |
|---|---|---|---|
| 唯一主刀 | **立刻**做 2024 第三窗预锁设计（`PR68-R3`）；不先补本窗 S3/切片 | **先砍本窗两笔零回测债（R2-D）**；第三窗本周同步设计但不定稿 | **取 Opus 的动手顺序 + Codex 的并行设计**：动手 = R2-D；设计轨 = R3 预锁包 |
| 理由焦点 | S4 过 ⇒ 训练紧迫性下降；最大未知是迁移性 | S4 口径比 S1/S2 **松**（全窗一点 vs 两段 Δ）；未对称段检就把 `10/3+ST` 锁成第三窗主臂过早 | **采纳 Opus 的严格度警告**：先对称化再定第三窗主臂 |
| 「S4 过 → 训练紧迫性下降」 | 直接用 | 只对「训练作收益杠杆」成立；对「训练作第三窗前置」须撤回（2024 ∈ 模型 B 训练段 ⇒ 第三窗必重训） | **并版写清两层**：收益杠杆可押后；第三窗物理上仍要重训，预锁本周做完 |

不立刻开第三窗训练；不「把本窗欠账全补」（`20/3 全关补段` 对照登记不跑）；不改道（改道条件 = 线上宽度段检 G2/G3 失败）。

---

## 3. 三问并版答

### Q1 · 下一刀

**动手主刀 = R2-D（零回测）**

| 步 | 内容 | 成本 |
|---|---|---|
| D1 | `10/3 全关` 强段 104 / 补段 66 官方切片，与 `10/3+ST` 对齐 → S1′/S2′ | 纯切片 |
| D2 | 用「成本 = 换手 × 常数」闭式还原 realistic 日净值 → S3；校验复现官方 +14.54% / −0.05%（≤0.3pp） | 纯算术；失败才允许一趟 replay |

成功门（钉死）：G1 切片锚；G2=S1′；G3=S2′；G4=S3；G5=全过 → `10/3+ST` 记「线上宽度 · 待第三窗验证增量」，**仍不改线上**。

失败标签：`SEG_ANCHOR_UNVERIFIED` / `ST_GATE_SUBPERIOD_ONLY_ONLINE_WIDTH`（→ 改道，第三窗主臂换训练侧臂 B）/ `ST_GATE_COST_FRAGILE` / `COST_DERIVATION_INVALID` / `INCONCLUSIVE_SINGLE_WINDOW`。

**并行设计轨 = R3 预锁（本周只设计）**

- 测试窗 `2024-01-01～2024-12-31`；purged；关早停；固定轮数（三项必须写成确切值，否则 `DOC_GATE_INCOMPLETE`）。
- 臂 A 原宇宙 / 臂 B 剔 ST(PIT)+上市未满 60 日；主判格 10/3 ±ST；50/5 仅诊断不进 shortlist。
- 三门：收益符号 / 排序增量 / 现实档。失败按 Codex 二阶标签分流。

### Q2 · 成本是否单独开刀

**否。** 记 `10/3+ST` 为成本敏感型研究增量；把换手预算 / DistanceToPositive / 现实档门写进 R2-D 与 R3 预锁。`88.5×` ≈ 10/3 结构上限的 87%，信号层「审换手」≈ 拧 n_drop → 撞 N7。realistic 拖累 19.61pp 紧贴 20pp 的 `COST_GAP_EXECUTION` 门，**尚未触发**。

### Q3 · 训练侧去 ST

**仍与 2024 第三窗合并、本周只设计。** 不因 S4 立刻开训。若 R2-D 打出 `ST_GATE_SUBPERIOD_ONLY_ONLINE_WIDTH`，第三窗主臂改为臂 B（训练侧），推断期闸降为对照——那是证据逼出来的改道，不是现在拍板。

---

## 4. 明确拒绝

不改线上 10/3；不覆盖 `8a061ea4` pred/原包；不开买入状态；不抬 `50/5+ST`；不扫 n_drop/宽度；不单独开成本刀；本周不开第三窗训练；不把本页数字写进「可上线」。

---

## 5. 三句交付

1. 动手 = R2-D（S1′/S2′ + 闭式 S3）；设计 = 2024 第三窗预锁并行；先对称口径再定第三窗主臂。  
2. `+20.7% / 88.5×` 只记研究增量；成本不单独开刀，并进门。  
3. 训练侧去 ST 仍合并第三窗、本周只设计；线上仍 10/3。
