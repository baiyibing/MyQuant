## 下一刀（唯一主刀）

**裁决：唯一主刀是 `PR68-R3`——立刻完成 2024 独立第三 OOS 窗预锁设计，本周只设计、不跑。** 不先补本窗 S3、官方补段对照或单独成本审计。

理由是两项结果必须同时解释：

- **S4 通过**：`10/3+ST` 在当前窗确实作用于线上宽度，训练侧紧迫性下降。
- **S1/S2 不过**：`50/5+ST +23.2%` 已被判为 `ST_GATE_SUBPERIOD_ONLY`，补本窗欠账不能把它重新抬进 shortlist。
- 因而当前最大未知不是“本窗还能补出多少”，而是 `10/3+ST` 的研究年化 lift 能否迁移到真正独立、未被早停消费的窗口。

预锁格子如下，全部报 zero / qlib_default / realistic、换手和回撤：

| 测试窗 | 训练臂 | 宽度 | 推断开关 | 定位 |
|---|---|---|---|---|
| 2024 独立 OOS | A：原训练宇宙 | 10/3 | 全关、ST+年龄 | 唯一主判格 |
| 2024 独立 OOS | B：训练侧剔 ST（PIT）及上市未满 60 日 | 10/3 | 全关、ST+年龄 | 训练侧迁移判别 |
| 2024 独立 OOS | A、B | 50/5 | 全关、ST+年龄 | 宽度稳健性诊断，不进入 shortlist |

不纳入 20/3，不扫 topk×n_drop。

**2024 切分预锁：**

- 测试段锁为 `2024-01-01～2024-12-31`；2024 数据不得进入训练、valid、早停、规则或轮数选择。
- 训练和任何 valid 数据必须全部截止在 2024 之前；测试前按标签跨度留 purge。
- 关 early stopping，轮数在开跑前固定；A/B 除训练宇宙外全部冻结。
- 现有 handoff/run 页没有给出训练起止日期、固定轮数和 purge 交易日数；PR #68 设计页必须先把三项写成确切值，否则标 `DOC_GATE_INCOMPLETE`，不得开跑。

**预锁三门：**

1. **收益符号门**：2024 主判格 `10/3+ST` 的 qlib 超额及等权超额为正。
2. **排序／增量门**：同一训练臂、同一 pred 内，`10/3+ST` 不弱于 `10/3` 全关；A/B、开关之间只做受控对照，不与旧 recorder 数字相加。
3. **现实档门**：`10/3+ST` realistic 超额为正，且相对全关的增量不翻负。

三门全过，只升为**跨窗研究候选**，不自动修改线上默认。

失败标签按二阶归因：

- qlib／排序门失败：`WINDOW_DEPENDENT`。
- qlib 或 zero 尚存、realistic 失败：先打 `ST_GATE_COST_FRAGILE`；年化成本缺口超过既定 20% 门再打 `COST_GAP_EXECUTION`。
- 独立窗重复出现“研究档存活、现实档失效”才升级为 `COST_STRUCTURE_UNSAVEABLE`，转执行层处理。
- 训练臂 B 两个宽度均不优于 A：`TRAIN_UNIVERSE_NO_TRANSFER`。
- B 降低头部 ST 占比却同时损伤秩 1–10 次日超额：`TRAIN_UNIVERSE_HEAD_DAMAGE`。

## 10/3+ST +20.7%（换手 88×）与成本是否单独开刀

记为：**“线上宽度待独立窗验证的、成本敏感型研究增量”**。

它不是可上线方案，也不是一般性的 ST 闸结论。当前证据只说明：

- 同窗 qlib 超额由全关 `+3.0%` 到 `+20.7%`，S4 通过；
- 两个不相交子段约 `+22%`，排除了单一子段独占贡献；
- 但两段仍共同属于同一个 2026 全窗，不能替代独立 OOS；
- 换手由 `43.0×` 增至 `88.5×`，回撤由 `−37.8%` 加深至 `−50.2%`；
- realistic 仍为 `+14.5%`，但成本拖累已达 `19.6pp`。

**不单独开成本刀。** 当前窗已经有三档成本，继续围绕同窗拆 S3 或成本结构，只能提高测量精度，不能回答迁移性。把成本完整并入 2024 预锁包，在同一独立窗同时判断收益符号、增量和现实档；若失败，再按上述成本标签分流。

## 训练侧去 ST 是否仍与 2024 第三窗合并（本周只设计？）

**是，维持合并；本周只设计，不提前重训。**

S4 通过后，没有证据逼迫立即改训练宇宙。此时单独训练会产生新 pred，破坏当前 `8a061ea4` 的同 pred 推断开关对照，也容易用 2026 表现反向标定剔除规则。

两臂保持原设计：

- A：原训练宇宙。
- B：按 PIT 剔除 ST，并剔除上市未满 60 个交易日的样本。
- 两臂均关早停、固定轮数、purged；特征、阈值、种子、数据处理和策略参数全部冻结。

训练侧成功判据沿既有 Q3：

- B 在 2024 的 10/3、50/5 两个宽度上，qlib 超额都不低于 A；
- 秩 1–20 的 ST 占比由既有约 `21%` 降至 `<5%`；
- 秩 1–10 次日超额不下降。

其中 50/5 仅用于检查训练侧是否跨宽度迁移，不代表恢复 `50/5+ST` 的候选资格。

## 纪律提醒（线上仍 10/3；不覆盖 8a061ea4 pred；不开买入状态；50/5+ST 不抬回 shortlist）

- 线上默认仍为 **10/3 全关**；本轮只研究年化 lift。
- 不覆盖 recorder `8a061ea428e04bb3a199a485ade49d0e` 的 `pred.pkl`、既有分析包或其他当日产物。
- 不开 `--buy-state-filter`；任何格子若靠买入状态才过门，直接判失败。
- `50/5+ST` 已因 S1/S2 失败打 `ST_GATE_SUBPERIOD_ONLY`，不抬回 shortlist。
- 不用单窗峰值修改默认；不扫 20/3，不扫 n_drop，不下调 realistic 成本。
- 训练臂、推断开关及旧 recorder 结果不得相加、相乘或包装成同一基线。

## 证据锚点（只引用 handoff/run 页数字，不编造）

- `2026-09-15-pr67-round2-handoff.md`：10/3 全关／10/3+ST 的 qlib 超额分别为 `+3.0% / +20.7%`；绝对净年化 `+0.1% / +17.8%`；回撤 `−37.8% / −50.2%`；换手 `43.0× / 88.5×`。
- `2026-09-15-10n3-st-age.md`：10/3+ST 三档超额为 zero `+33.0%`、qlib `+20.7%`、realistic `+14.5%`；qlib／realistic 成本拖累 `13.1pp / 19.6pp`；强段 `+21.4%`、补段 `+22.8%`。
- `2026-09-15-pr67-p1-2-and-segments.md`：50/5+ST 补段 `+13.8%`，低于全关补段 `+17.7%`；强段 Δ_ST `+22.3pp`、补段 Δ_ST `−3.9pp`，故 S1/S2 不过；S4 通过。
- 同页 P1-2：50/5 全关失资格仍持仓 `20.7%`，`84/169` 天超过 20%；卖出 420 笔中 87 笔失资格，开闸后强制出局约 `1%`，因此继续禁止 n_drop 细表。
- `2026-09-15-pr67-dual-synth-verdict.md` 与 `2026-09-15-pr67-cursor-opus-verdict.md`：2024 第三窗采用关早停、固定轮数、purged 的 A/B 两臂设计；训练臂 B 为剔除 ST（PIT）及上市未满 `60` 日，头部诊断门为秩 1–20 ST 占比从约 `21%` 降至 `<5%`。