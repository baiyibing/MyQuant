# PR #68 · R3 预锁 · 2024 第三窗两臂重训

**日期**：2026-09-15 晚  
**状态**：预锁三项已写成确切值；R2-D 已出（G3/G5 不过）→ **主臂 = 臂 B**。  
**线上默认**：仍 10/3。本页全过也不自动改默认。  
**不覆盖**：`8a061ea4` 的 `pred.pkl` / 原分析包。

---

## 切分（钉死）

标签 = Alpha158 `Ref($close, -2) / $close − 1`，horizon = **2** 个交易日。  
purge = horizon + 5 = **7** 个交易日。  
valid 末日 2023-12-20 + 7 个交易日 = 2023-12-29；2024 首个交易日 2024-01-02。

| 段 | 日期 | 说明 |
|---|---|---|
| train | **2020-01-01～2022-12-30** | 全部在 2024 之前 |
| valid | **2023-01-03～2023-12-20** | 只作对照打印；早停已关，不用于选轮数 |
| test | **2024-01-02～2024-12-31** | 独立 OOS；2024 不进训练 / valid / 早停 / 轮数 |

## 模型（钉死）

对齐 `8a061ea4` 的 `best_iteration`：**223** 棵。  
qlib `LGBModel` 默认 `num_boost_round=1000` + `early_stopping_rounds=50`。本窗关早停的实现：

`--num-boost-round 223 --early-stopping-rounds 223`

（回调在 223 棵之前不会触发，等价固定轮数。）

其余 LGB 超参与现役训练脚本一致（lr 0.0421 / max_depth 8 / num_leaves 210 / …）。seed 不另设。  
**不开** `--buy-state-filter`。涨停 learn / 拒单维持默认。

## 两臂

| 臂 | 训练宇宙 | CLI | 定位 |
|---|---|---|---|
| A | 原宇宙 `market=all` | 不加 `--tradable-universe` | sanity / 对照。先过窗内可复现才读 B |
| B | `all_tradable`（ST 黑名单 + 上市未满 60 日顺延） | `--tradable-universe` | **本轮主臂**（R2-D 打出 `ST_GATE_SUBPERIOD_ONLY_ONLINE_WIDTH`） |

推断期每臂各报：`10/3` 全关、`10/3+ST`（`--st-filter --age-filter`）。  
`50/5` 只诊断、不进 shortlist。不扫 20/3，不扫 n_drop。

跨 recorder **只比符号与排序**，不与 `8a061ea4` 的 +20.7% 相加（N2）。

## 门（沿 Opus T1–T4 + Codex 三门）

| 门 | 判据 | 失败 |
|---|---|---|
| T1 | 2024 `10/3+ST` qlib 超额 > 0，且 ≥ 同臂 `10/3` 全关 +3pp；H1/H2（2024 上半年 / 下半年）同号且各自 Δ_ST ≥ +3pp | `ST_GATE_WINDOW_DEPENDENT` / `ST_GATE_SUBPERIOD_ONLY_2024` |
| T2 | 2024 realistic 超额 > 0 且 Δ_ST(realistic) ≥ 0；年化换手 ≤ 170×；realistic 拖累 < 20pp | `ST_GATE_COST_FRAGILE` / `COST_GAP_EXECUTION` |
| T3 | 臂 B 的 `10/3` 全关超额 ≥ 臂 A 同格 +3pp；头部可填席不降 | `TRAIN_UNIVERSE_NO_TRANSFER` / `TRAIN_UNIVERSE_HEAD_DAMAGE` |
| T4 | 2024 `10/3+ST` 超额最大回撤不深于 −45% | `RISK_BUDGET_BREACH` |

T1–T4 全过 → 升为「线上候选（待并版）」；**仍不改线上**。

## 开跑命令（臂 A 先行）

```bash
cd D:\PycharmProjects\MyQuant\my_scripts
D:/anaconda3/envs/vanna312/python.exe -u custom_train_backtest.py ^
  --train 2020-01-01:2022-12-30 ^
  --valid 2023-01-03:2023-12-20 ^
  --test 2024-01-02:2024-12-31 ^
  --topk 10 --n-drop 3 ^
  --num-boost-round 223 --early-stopping-rounds 223 ^
  --handler-cache
```

臂 B：同上，加 `--tradable-universe`。新 recorder，禁止写回 `8a061ea4`。
