# Codex / Cursor Opus · PR #67 第二轮接手 · 刀 1 与 P1-2 已跑完

**给谁**：Codex（`gpt-5.6-sol` + high）和 Cursor Opus 5，分析-only 优先；要跑数须同 pred、不覆盖今日产物、不改线上默认。  
**日期**：2026-09-15 晚  
**本机**：`D:\PycharmProjects\MyQuant`（Windows）  
**主 pred**：recorder `8a061ea428e04bb3a199a485ade49d0e`（模型 B；窗 2026-01-05～09-14）  
**线上默认**：**维持 10/3**。本页任何数字都不够改 topk / 开过滤默认 / 把 `10/3+ST` 写成已上线。

上一轮并版：`2026-09-15-pr67-dual-synth-verdict.md`。  
本轮实跑：`2026-09-15-10n3-st-age.md` + json；`2026-09-15-pr67-p1-2-and-segments.md` + json。

请在本页 + 上列 run 页上裁「再怎么改、下一刀砍哪条」。不要另开异 seed 训练，不要覆盖 `exports/analysis/8a061ea428e04bb3a199a485ade49d0e/` 或该 recorder 的 `pred.pkl`。

---

## 请两边各答的三问

1. **下一刀该砍哪条**？并版当时写：S4 过 → 训练紧迫性下降、优先独立第三窗；S1/S2 不过 → `+23.2%` 降为单段效应。现在两件事同时发生了。是立刻设计 / 开第三 OOS 窗，还是先补本窗欠账（S3 现实档切片、全关宽度补段官方对照、10/3+ST 换手 88× 的成本结构），还是改道？写出成功判据和失败标签。
2. **`10/3+ST` 全窗 +20.7%（现实档仍 +14.5%，两段同号约 +22%）** 记成什么？并版只允许「线上宽度待验证增量 / 研究档」，禁止当可上线。换手从 43× 翻到 88×、回撤 −50.2%。要不要单独开一刀审它的成本，还是把成本并进第三窗预锁包？
3. **训练侧去 ST** 还按「与 2024 第三窗合并一次两臂重训、本周只设计」吗？S4 过了，训练是不是继续押后？若你改口，必须写清被哪一格证据逼出来，且不得毁掉同 pred 对照（N2）。

不要用买入状态凑数。不要提议单窗改线上 10/3。不要把 50/5+ST 再抬回 shortlist，除非你能推翻 S1/S2 的切片口径并给出替代判据。

---

## 硬纪律（页眉钉死）

| ID | 禁令 |
|---|---|
| N1 | 旧过滤开 +9.1% 禁当已验证基线；不得与今日数字相加 |
| N2 | 跨 recorder / 跨日历表默认可加、可乘 |
| N3 | 单窗改线上 topk 或默认开过滤（含「先把 10/3+ST 写成默认」） |
| N4 | 下调 realistic 让策略转正 |
| N7 | 单窗加大 n_drop 救现实档；P1-2 全关失资格仍持仓 20.7% → **n_drop 细表继续禁** |
| 产物 | 禁止覆盖今早 50/5 `pred.pkl`、原分析包、`预测结果.csv`；新跑必须另目录 / 另戳文件名 |
| 开关 | 现成 CLI：`--st-filter` `--age-filter`；**不要开** `--buy-state-filter` |

已打标签：`WINDOW_DEPENDENT`、**`ST_GATE_SUBPERIOD_ONLY`**（50/5+ST 增量）。  
未打：`ST_GATE_WIDTH_COUPLED`、`COST_GAP_EXECUTION`、`COST_STRUCTURE_UNSAVEABLE`、`ST_GATE_COST_FRAGILE`（S3 未测）。

---

## 1. 本轮刚跑完的账（只读这些）

同 pred `8a061ea4`，仅 ST+年龄，买入状态关。超额 = qlib_default vs 沪深300 年化，除非另注。

### 1.A 刀 1 第一格 · 10/3+ST

| | 10/3 全关 | **10/3+ST** | 20/3+ST | 50/5+ST |
|---|---|---|---|---|
| 超额 vs 300 | +3.0% | **+20.7%** | −3.8% | +23.2% |
| 绝对净年化 | +0.1% | +17.8% | −6.6% | +20.3% |
| 回撤 | −37.8% | **−50.2%** | −53.9% | −33.1% |
| 窗内换手 | 43.0× | **88.5×** | 47.9× | 32.4× |

三档：zero +33.0% / qlib +20.7% / realistic +14.5%。成本拖累 qlib 13.1pp / realistic 19.6pp。  
不相交切片（日均×252）：强段 104 **+21.4%** / 补段 66 **+22.8%**。两段同号，不是单段幻觉。  
**S4 通过。未打 `ST_GATE_WIDTH_COUPLED`。**  
分析包：`exports/analysis/8a061ea4_10n3_st_age/`（不要覆盖）。

先前担心「闸只在宽名单兑现、10/3 开闸会像 20/3 一样被挖空」——在本窗不成立。窄名单去掉头部 ST 后仍能填满 10 席。宽度×过滤仍是组合单元，只是单元里 **10/3+ST 与 20/3+ST 方向相反**。

### 1.B 不相交二段 · 50/5 / 20/3（已有账切片）

| 账本 | 全窗切片 | 强段 104 | 补段 66 |
|---|---|---|---|
| 50/5 全关 | +12.5% | +9.1% | **+17.7%** |
| 50/5 ST+年龄 | +24.6% | **+31.5%** | +13.8% |
| 50/5 三闸 | +10.0% | +3.7% | +19.9% |
| 20/3 ST+年龄 | −4.0% | **−25.7%** | +30.3% |

官方全窗锚：50/5 全关 +11.8%；50/5+ST +23.2%；20/3+ST −3.8%。切片同号同序，与官方差约 1pp。

| 代号 | 判据 | 结果 |
|---|---|---|
| S1 | 50/5+ST 两段都 > 0，且都 ≥ 同段 50/5 全关 | **不过**。补段 +13.8% < 全关补段 +17.7% |
| S2 | Δ_ST 两段都 ≥ +3pp | **不过**。强段 +22.3pp；补段 **−3.9pp** |
| S3 | 两段 realistic > 0 且 Δ_ST≥0 | **未测**（包无 realistic 日净值） |
| S4 | 10/3+ST 全窗 qlib ≥ +3.0% | **通过** |

**S1–S4 没有全过 → 「宽名单 + ST」仍不进 shortlist。**  
+23.2% 是强段效应：闸在 4–8 月兑现，补段反而弱于全关。

### 1.C P1-2 持仓路径（纯 join，零回测）

| 账本 | 持仓名日 | 失资格占比 | 失资格>20% 天数 | 卖出里失资格 |
|---|---|---|---|---|
| 50/5 全关 | 8838 | **20.7%** | **84 / 169** | 20.7% |
| 50/5 三闸全开 | 8469 | 0.26% | 0 | 1.1% |
| 50/5 ST+年龄 | 8493 | 0.14% | 0 | 1.2% |
| 20/3 ST+年龄 | 3395 | 0.27% | 0 | 1.1% |

全关 `held_not_topk` 7060 名日，秩 P50 **1207** / P90 4332；其中 ST 1369（19%）= 20.7% 脏仓主来源。卖出 420 笔里 87 笔当天已失资格（强制出局 21%），其余是排序轮换。开闸后强制出局 ~1%，多出来的换手几乎全是 walk-down。  
**硬闸触发：禁止先跑 n_drop 细表。**

---

## 2. 并版当时怎么说「跑完之后」

摘自 `2026-09-15-pr67-dual-synth-verdict.md` §5，供对照，不是新裁决：

- 若 **S4 过**：闸有通用性，训练紧迫性下降，**优先补独立窗**。
- 若 **S4 不过**：打 `ST_GATE_WIDTH_COUPLED` → 训练侧成为抬 10/3 的主路径。
- S1/S2 不过 → `ST_GATE_SUBPERIOD_ONLY`，+23.2% 不得当新基线。
- R3（本周只设计）：2024 第三 OOS ⊕ 训练侧去 ST，两臂一次重训；关早停/固定轮数、purged。

实际是 **S4 过 + S1/S2 不过** 同时成立。请不要假装并版已经回答了「同时成立」之后砍哪条。

---

## 3. 本窗还缺、但不是默认下一刀

这些可以做，但请明确写「现在做 / 并进下一刀 / 不做」：

| 欠账 | 现状 |
|---|---|
| S3 两段 realistic | 现有包没有分档日净值；要测得另 replay 或从三档 PortAna 拆日 |
| 10/3、20/3 全关补段官方 `risk_analysis` | 只有 50/5 全关补段切片 +17.7%；10/3 全关补段 Opus 曾反推约 −13%，未官方核 |
| 20/3 全关补段 | 对照缺 |
| `10/3+ST` 换手 88× 的 P1-3 预算 | 现实档仍 +14.5%，但拖累 19.6pp；未打成本标签 |

---

## 4. 明确拒绝（沿用并版）

不改线上 10/3；不覆盖今早 `pred.pkl` / 原分析包；不开买入状态；不加 n_drop 救现实档；不把 +23.2 与旧 +9.1 相加；不写新过滤包装脚本；不在今日窗再扫 topk×n_drop；不把强段当第二独立窗；不把本页数字写进「可上线」叙事；不把 `10/3+ST +20.7%` 写成已经替换默认。

---

## 5. 请交付

1. 一页裁决：下一刀 ID、为什么不是相邻刀、成功/失败标签。  
2. 若要跑：格子（窗 × topk × 开关 / 训练臂），**用现成 CLI，不新写脚本**。  
3. 若要设计第三窗：预锁三门 + 失败标签二阶（沿 P0-1），写清 2024 切分与是否绑训练去 ST。  
4. 两边写完后仍由宿主并版，不各自改线上。
